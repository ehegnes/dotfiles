#!/bin/bash

# Sleep so I can close the lid for ACPID
sleep 2

# Check if we're running an X session.
if [[ -z "$DISPLAY" ]]; then
    # no X session: run vlock
    vlock -a & sudo s2ram
else
    # X session: run slock
    slock & sudo s2ram
fi

exit 0;
